# Ignore Visual Studio temporary files, build results, and
# files generated by popular Visual Studio add-ons.

# User-specific files
*.rsuser
*.suo
*.user
*.userosscache
*.sln.docstates

# User-specific files (MonoDevelop/Xamarin Studio)
*.userprefs

# Build results
[Dd]ebug/
[Dd]ebugPublic/
[Rr]elease/
[Rr]eleasePublic/
x64/
x86/
build/
bld/
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/

# Visual Studio 2015/2017 cache/options directory
.vs/
.vscode/

# NuGet packages
*.nupkg
*.snupkg
# NuGet local cache
# Uncomment if you don't want to include the NuGet cache
# .nuget/

# Npm packages (if using npm)
node_modules/

# Visual Studio Code settings
.vscode/

# Resharper files
_ReSharper.*/
*.[Rr]e[Ss]harper
with tpc as (
--     select count(distinct player_id) as total
--     from Activity
-- ),
-- logged_two_days_users as (
--     select player_id,
--     min(event_date) first_login_date,
--     lead(event_date, 1) over(partition by player_id order by event_date) next_login_date
--     from Activity
--     group by player_id
-- )
-- select round(count(*) / (select total from tpc), 2) as fraction
-- from logged_two_days_users
-- where logged_two_days_users.next_login_date IS NOT NULL
-- and datediff(logged_two_days_users.next_login_date, logged_two_days_users.first_login_date) = 1



# User-specific files
*.suo
*.user
*.userosscache
*.sln.docstates

# Visual Studio profiler
*.psess
*.vsp
*.vspx
*.tmp

# Others
*.vspscc
*_i.c
*_p.c
*_i.h
*.cache
*.bak
*.log
*.vsmdi

# Rider
.idea/
*.sln.iml

# ASP.NET Scaffolding
ScaffoldingReadMe.txt

# DotCover
.idea/
*.dotCover

# MSTest test result
TestResult.xml
TestResult*.xml

# Coverlet
coverage.opencover.xml
coverage.json

# Files built by Visual Studio
*.dll
*.exe
*.pdb
*.cache
*.obj
*.ipch
*.aps
*.pch
*.vspscc

# Others
*.[Cc]ache
*.bak
*.backup
*.tmp